#ifndef INLINE
# if defined(__GNUC__) && (__GNUC__ > 3 || __GNUC__ == 3 &&  __GNUC_MINOR__ > 0) && defined(NDEBUG)
#  define INLINE __attribute__((always_inline)) inline
# else
#  define INLINE inline
# endif
#endif
